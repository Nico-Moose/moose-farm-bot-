# Safe patch: split 08-feature-overrides.js

Что сделано:
- `public/js/08-feature-overrides.js` разделён на маленькие файлы `08a`–`08i`.
- Порядок подключения в `public/farm.html` сохранён, чтобы override-логика работала как раньше.
- Старый `08-feature-overrides.js` оставлен заглушкой.
- CSS, API, рынок, здания, рейды и админ-логику по смыслу не переписывали.

Важно:
- Заменять нужно весь ZIP-патч целиком, а не только один файл.
- Это рефактор структуры, визуальных изменений быть не должно.
