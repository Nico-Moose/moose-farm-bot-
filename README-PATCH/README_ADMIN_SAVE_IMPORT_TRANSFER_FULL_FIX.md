# Admin Save / Import / Transfer Full Fix

Дата: 2026-05-03

Что исправлено:
- Поля игрока в админке снова реально сохраняются.
- Исправлен broken route `POST /player/set-field`:
  - убраны несуществующие функции `normalizeLogin`, `getDb`, `createAdminBackup`, `logAdminAction`, `adminProfileView`
  - теперь используется `saveFarmBackup`
  - после сохранения возвращается свежий профиль из базы
- Поле `parts` теперь сохраняется и в колонку `parts`, и в `farm_json.resources.parts`.
- Поле `level` теперь сохраняется и в колонку `level`, и в `farm_json.level`.
- Перенос фермы теперь умеет создавать целевого игрока, даже если он ещё не заходил на сайт.
- README лежит в `README-PATCH`.

Что это чинит:
- меняешь уровень / баланс / защиту / рейд-силу / лицензии / турель → жмёшь обновить → значения не откатываются назад
- перенос фермы old -> new больше не падает только из-за отсутствия целевого профиля
