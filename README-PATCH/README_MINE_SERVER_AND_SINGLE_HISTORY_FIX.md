# Mine server + single history fix

Сделано:
- исправлен серверный баг условий шахты: требование 200/200 теперь включается только на уровнях 76-100, а не с 2 уровня шахты.
- исправлены source для async sync: рейд-сила больше не помечается как turret_upgrade.
- успешный фоновый async sync больше не пишет отдельную техническую запись в журнал.
- журнал на фронте скрывает старые sync-записи и показывает одну понятную строку по факту действия.
- сохранены прошлые UI-фиксы: без пустой полоски, без `У тебя`, без `цена пачки`, порядок зданий завод/фабрика/шахта.

Не трогалось:
- экономика.
- формулы цен.
- логика списания ресурсов.
