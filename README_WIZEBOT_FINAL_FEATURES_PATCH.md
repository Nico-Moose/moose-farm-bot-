# Raid turret and balance clarity fix

Исправлено:

- Если турель цели сработала, рейд считается отбитым.
- При отбитом рейде цель не теряет farm_balance и upgrade_balance.
- С атакующего списывается штраф турели (`turret_refund` / `attacker_loss`).
- В popup и истории теперь пишется не `успех`, а `турель отбила атаку`.
- История рейдов больше не показывает сырой JSON, а выводит понятное описание.
- Подпись `Всего монет` заменена на `Доступно для трат`, потому что это сумма фермерского и ап-баланса для проверок стоимости.
