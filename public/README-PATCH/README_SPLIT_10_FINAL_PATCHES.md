# Safe patch: split `public/js/10-final-patches.js`

Что сделано:
- большой файл `public/js/10-final-patches.js` разделён на отдельные модули `public/js/10a-*.js` ... `public/js/10i-*.js`;
- порядок выполнения сохранён тем же, что был в исходном файле;
- `public/farm.html` теперь подключает эти файлы по очереди;
- старый `10-final-patches.js` оставлен как маленький compatibility placeholder;
- логика, CSS, API, рынок, здания, рейды и админка не переписывались.

Важно:
- ставить патч целиком, вместе с `public/farm.html` и всеми новыми файлами из `public/js/`;
- если заменить только один файл, подключение модулей может не сработать.
